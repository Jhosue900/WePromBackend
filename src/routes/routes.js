const { Router } = require("express")
//const { makeOrder, makeReservation, sendMessage, showMessages, showOrders, showReservations } = require("../controllers/controller")

const router = Router()


// Client services
//router.post('/makeReservation', makeReservation)
//router.post('/makeorder', makeOrder)
//router.post('/sendMessage', sendMessage)

// Dashboard services routes

//router.get('/showReservations', showReservations)
//router.get('/showOrders', showOrders)
//router.get('/showMessages', showMessages)


module.exports = router